# vim: filetype=conf

# Fish Abbr List
# General Purpose Bindings
c	"clear"
e	"$EDITOR"
h	"echo 'Hello world'"
f	"fortune | lolcat"
l	"ls"
r	"ranger"
v	"nvim"
x	"sxiv -ft *"
co	"code ."
py	"python"
ka	"killall"
rd	"rm -rf"
wc	"nmcli con up DexterWifi_wlink"
YT	"youtube-viewer"
ipy	"ipython"
mkd	"mkdir -pv"
jpn	"jupyter-notebook"
grep	"grep --color=auto"
diff	"diff --color=auto"
https	"http --default-scheme=https"

# Sudo Requiring Bindings
sd	"sudo"
SS	"sudo systemctl"
sv	"sudo nvim"
sdn	"sudo shutdown now"
npm	"sudo npm"
sdn	"sudo shutdown now"
pip	"sudo pip"
tlp	"sudo tlp"
snap	"sudo snap"

# pacman-yay Shortcuts
p	"sudo pacman"
y	"yay"
pi	"sudo pacman -Sy"
pr	"sudo pacman -Rns"
yi	"yay -Sy"
yr	"yay -Rns"
lsp	"pacman -Qett --color=always | bat"
lspy	"pacman -Qm --color=always | bat"
orp	"sudo pacman -Rsun (pacman -Qqtd)"

# git Shortucts
g	"git"
ga	"git add"
gp	"git pull"
gc	"git commit"
gl	"git log"
gs	"git status"
gcl	"git clone"

# Docker Shortcuts
da	"docker attach"
db	"docker build"
ds	"docker start"
di	"docker images"
dr	"docker run"
dk	"docker kill"
dca	"docker container ps --filter 'status=running'"
dcd	"docker-compose down"
dci	"docker container ps --filter 'status=exited'"
dcr	"docker container rm"
dcu	"docker-compose up"
dir	"docker image rm"
dst	"docker stop"
dvp	"docker volume prune"
dvr	"docker volume rm"

# Yarn/npm/npx Shortcuts
yd	"yarn dev"

# pipenv Shortcuts
psh	"pipenv shell"

# Custom Bindings for taskbook
tbe	"tb -e @"
tba	"tb -t"
tbg	"tb -t @"
tbd	"tb -d"
tbn	"tb -n"
tbc	"tb -c"
tbh	"tb -h"
tbp	"tb -p @"
tbng	"tb -n @"
tbcc	"tb --clear"

# Custom Scritps
se	"du -a ~/Documents/linux-worker/dotfiles/scripts/* ~/.config/* | awk '{print \$2}' | fzf | xargs -r $EDITOR" # Scripts Folder Search tool
vf	"xdg-open (fzf) 2>/dev/null" # Any folder Search Tool
yt	"youtube-dl --add-metadata -i" # Youtube Video Download
yta	"youtube-dl --add-metadata -i -x -f bestaudio/best" # Youtube Audio Only Download
