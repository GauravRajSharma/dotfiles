# vim: filetype=conf

# Fish Abbr List
# General Purpose Bindings
b	"bat"
c	"clear"
e	"$EDITOR"
l	"ls"
v	"nvim"
co	"code ."
py	"python3"
ka	"killall"
rd	"rm -rf"
ipy	"ipython"
mkd	"mkdir -pv"
jpn	"jupyter-notebook"
grep	"grep --color=auto"
diff	"diff --color=auto"
https	"http --default-scheme=https"

# Sudo Requiring Bindings
sd	"sudo"
SS	"sudo systemctl"
sv	"sudo nvim"
sdn	"sudo shutdown now"
npm	"sudo npm"
sdn	"sudo shutdown now"
pip	"sudo pip3"
tlp	"sudo tlp"

# git Shortucts
g	"git"
ga	"git add"
gp	"git pull"
gc	"git commit"
gl	"git log"
gs	"git status"
gps	"git push"

# Docker Shortcuts
da	"docker attach"
db	"docker build"
ds	"docker start"
di	"docker images"
dr	"docker run"
dk	"docker kill"
dca	"docker container ps --filter 'status=running'"
dcd	"docker-compose down"
dci	"docker container ps --filter 'status=exited'"
dcp	"docker container prune"
dcr	"docker container rm"
dcp	"docker container prune"
dcu	"docker-compose up"
dir	"docker image rm"
dst	"docker stop"
dvp	"docker volume prune"
dvr	"docker volume rm"

# Pipenv Shortcuts
pipsh	"pipenv shell"
pipi	"pipenv install"

# Custom Bindings for taskbook
tbe	"tb -e @"
tba	"tb -t"
tbg	"tb -t @"
tbd	"tb -d"
tbn	"tb -n"
tbc	"tb -c"
tbh	"tb -h"
tbp	"tb -p @"
tbng	"tb -n @"
tbcc	"tb --clear"

# Custom Scritps
se	"du -a ~/Documents/worker/dotfiles/scripts/* ~/.config/* | awk '{print \$2}' | fzf | xargs -r $EDITOR" # Scripts Folder Search tool
vf	"xdg-open (fzf) 2>/dev/null" # Any folder Search Tool
